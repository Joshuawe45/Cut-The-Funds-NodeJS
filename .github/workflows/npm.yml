name: npm audit

on:
  push:

jobs:
  checkout-repo:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout 🛎️
      uses: actions/checkout@v2
    - name : Installing  tool
      run : |
        cd $GITHUB_WORKSPACE
        npm install npm@latest –g
        npm audit
        

